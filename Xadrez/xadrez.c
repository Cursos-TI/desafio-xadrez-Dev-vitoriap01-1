#include <stdio.h>

// Desafio de Xadrez - MateCheck
// Este código inicial serve como base para o desenvolvimento do sistema de movimentação das peças de xadrez.
// O objetivo é utilizar estruturas de repetição e funções para determinar os limites de movimentação dentro do jogo.

int main() {
    // Nível Novato - Movimentação das Peças

     int Torre, Rainha, Bispo, i;
    // mover a torre 5 casas para a direita

    for (int i = 0; i < 5; i++ );
    {
        printf("Torre se move para a direita\n"); // Imprime a mensagem para cada movimento da torre
    }
    do
    {
        printf("Rainha se move para a direita\n"); // Imprime a mensagem para cada movimento da rainha
    }
    while(Bispo < 5);
    {
        printf("Bispo se move para cima a esquerda\n"); // Imprime a mensagem para cada movimento do bispo
        Bispo++;
    }

    
    return 0;
}
